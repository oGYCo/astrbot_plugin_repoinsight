{
  "api_base_url": {
    "type": "string",
    "description": "GithubBot 服务的 API 基础地址",
    "default": "http://api:8000",
    "hint": "请输入 GithubBot 服务的完整地址，如 http://localhost:8000"
  },
  "timeout": {
    "type": "int",
    "description": "API 请求的超时时间（秒）",
    "default": 30,
    "hint": "建议设置为 30-120 秒"
  },
  "poll_interval": {
    "type": "int",
    "description": "查询任务状态的轮询间隔（秒）",
    "default": 5,
    "hint": "建议设置为 3-10 秒"
  }
}